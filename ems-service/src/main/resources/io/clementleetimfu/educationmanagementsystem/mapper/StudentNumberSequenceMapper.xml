<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="io.clementleetimfu.educationmanagementsystem.mapper.StudentNumberSequenceMapper">

    <insert id="insertStudentNumberSequence">
        INSERT INTO student_number_sequence(intake_date, last_seq, create_time, update_time, is_deleted)
        VALUES (#{intakeDate}, #{lastSeq}, #{createTime}, #{updateTime}, #{isDeleted});
    </insert>


    <select id="selectStudentNumberSequenceByIntakeDate" resultMap="selectStudentNumberSequenceByIntakeDateResultMap">
        SELECT intake_date, last_seq
        FROM student_number_sequence
        WHERE intake_date = #{intakeDate}
        AND is_deleted = 0;
    </select>

    <resultMap id="selectStudentNumberSequenceByIntakeDateResultMap"
               type="io.clementleetimfu.educationmanagementsystem.pojo.entity.StudentNumberSequence">
        <result column="intake_date" property="intakeDate"/>
        <result column="last_seq" property="lastSeq"/>
    </resultMap>

    <update id="updateStudentNumberSequence">
        UPDATE student_number_sequence
        SET last_seq = #{seq}
        WHERE intake_date = #{intakeDate}
        AND is_deleted = 0;
    </update>
    
</mapper>