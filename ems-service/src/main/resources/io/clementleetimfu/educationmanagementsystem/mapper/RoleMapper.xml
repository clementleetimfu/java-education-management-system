<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="io.clementleetimfu.educationmanagementsystem.mapper.RoleMapper">

    <select id="selectRoleByName" resultType="io.clementleetimfu.educationmanagementsystem.pojo.entity.Role">
        SELECT id
        FROM role
        WHERE is_deleted = 0
        AND name = #{name}
    </select>

</mapper>