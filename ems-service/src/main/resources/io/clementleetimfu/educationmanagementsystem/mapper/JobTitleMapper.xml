<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="io.clementleetimfu.educationmanagementsystem.mapper.JobTitleMapper">

    <select id="selectAllJobTitle"
            resultType="io.clementleetimfu.educationmanagementsystem.pojo.vo.jobTitle.JobTitleFindAllVO">
        SELECT
        id,
        name
        FROM
        job_title
        WHERE
        is_deleted = 0
        ORDER BY
        name;
    </select>

</mapper>