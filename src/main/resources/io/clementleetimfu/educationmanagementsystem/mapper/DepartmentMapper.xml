<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="io.clementleetimfu.educationmanagementsystem.mapper.DepartmentMapper">

    <select id="findAllDepartment" resultMap="departmentResultMap">
        select id, name, create_time, update_time
        from department
        where is_deleted = 0
        order by update_time desc
    </select>

    <resultMap id="departmentResultMap"
               type="io.clementleetimfu.educationmanagementsystem.pojo.dto.department.DepartmentListDTO">
        <id property="id" column="id"/>
        <result property="name" column="name"/>
        <result property="createTime" column="create_time"/>
        <result property="updateTime" column="update_time"/>
    </resultMap>

    <delete id="deleteDepartmentById">
        update department
        set is_deleted = 1
        where id = #{id}
        and is_deleted = 0
    </delete>

    <insert id="addDepartment">
        INSERT INTO department (name, create_time, update_time, is_deleted)
        VALUES (#{name}, #{createTime}, #{updateTime}, #{isDeleted})
    </insert>

    <select id="findDepartmentById" resultMap="findByIdResultMap">
        select id, name, create_time, update_time
        from department
        where id = #{id}
        and is_deleted = 0
    </select>

    <resultMap id="findByIdResultMap"
               type="io.clementleetimfu.educationmanagementsystem.pojo.dto.department.DepartmentFindByIdDTO">
        <id property="id" column="id"/>
        <result property="name" column="name"/>
        <result property="createTime" column="create_time"/>
        <result property="updateTime" column="update_time"/>
    </resultMap>

    <update id="updateDepartment">
        UPDATE department
        set name = #{name}, update_time = #{updateTime}
        where id = #{id}
        and is_deleted = 0
    </update>

</mapper>